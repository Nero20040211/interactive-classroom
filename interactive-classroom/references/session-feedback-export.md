# Session feedback export

The learner-facing classroom may export `interactive-classroom-session-feedback`.

This file contains only observations explicitly entered by the learner in the current page session. It is not telemetry and it does not instruct the main Skill to modify itself.

The sibling `interactive-classroom-refiner` Skill may consume this export only when the user explicitly asks to improve or iterate the Skill.
