# proof-practice widget

Use after the learner has seen the proof or derivation. Supported modes: `ordering` and `reason-match`. Require stable step IDs, deterministic answers, dependency-aware feedback, and at least three steps.
