# causal-dag-lab

Use for explicit causal-assumption reasoning. Declare exactly one exposure and outcome, directed acyclic edges, adjustment candidates, valid adjustment sets, data/provenance note and causal-claim boundary. The DAG encodes assumptions; it does not discover causal truth from the picture.
