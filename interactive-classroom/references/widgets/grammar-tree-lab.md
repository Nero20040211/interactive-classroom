# grammar-tree-lab

Use for syntax/constituency relations. Declare one root, valid parent links, node roles and explanations. Treat the tree as an explicit teaching analysis; where theories differ, state the boundary rather than claiming one labeling scheme is universal.
