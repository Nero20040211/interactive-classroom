# cloze-lab

Use for context-sensitive language form selection. Config requires `segments` containing text atoms and blank objects with stable `id` + non-empty `answers`, plus `prompt` and at least two strategy cues. Accepted variants should be justified by language use; do not hide arbitrary exact-string rules.
