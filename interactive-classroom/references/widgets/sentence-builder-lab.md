# sentence-builder-lab

Use when constituent/token ordering is the learning target. Declare unique token IDs, a `targetOrder` using every token exactly once, and differentiated correct/incorrect feedback. Explain structure and meaning after checking; do not reduce grammar learning to token shuffling.
