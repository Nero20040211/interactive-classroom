# data-structure-lab

Computer Science native lab for deterministic stack/queue operations.

Required: `kind` (`stack` or `queue`), `initial`, `maxSize`, and a learner-facing prompt. The runtime must expose add/remove/reset operations, show current state and keep an operation log. Never imply a generic container has stack/queue semantics unless the chosen operation rule is explicit.
