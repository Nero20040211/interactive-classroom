# recursion-lab

Computer Science native lab for call-stack reasoning. Canonical runtime uses factorial recursion as a deterministic teaching model. Learners control `n` and step through call/return phases. The explanation must distinguish source-code recursion, call frames, the base case, and returned values.
